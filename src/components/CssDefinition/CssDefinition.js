import React from 'react';

const HtmlDefinition = () => {
    return(
        <div>
            <h2>Definition of CSS</h2>

            <p>From Wikipedia, the free encyclopedia. Cascading Style Sheets, or CSS, are a way to change the look of HTML and XHTML web pages. CSS was designed by the W3C, and is supported well by most modern web browsers. The current version of CSS is CSS3.</p>
        </div>
    );
}

export default HtmlDefinition;