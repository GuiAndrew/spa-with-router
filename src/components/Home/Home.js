import React from 'react';

const Home = () => {
    return(
        <div>
            <h2>Definition of Frontend Development</h2>

            <p>Front-end (computing), an interface between the user and the back end. Front-end processor (computer), a small-sized computer. Front-end processor (program) Front-end web development, the practice of producing HTML, CSS and JavaScript for a website or web application.</p>
        </div>
    );
}

export default Home;